name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container: devkitpro/devkitarm:latest
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Install dependencies
        run: |
          dkp-pacman -S --needed --noconfirm 3ds-curl 3ds-mbedtls 3ds-zlib 3ds-pkg-config || true
          apt-get update && apt-get install -y wget unzip || true

      - name: Install makerom and bannertool
        run: |
          wget https://github.com/3DSGuy/Project_CTR/releases/download/makerom-v0.18.3/makerom-v0.18.3-ubuntu_x86_64.zip
          unzip makerom-v0.18.3-ubuntu_x86_64.zip
          cp makerom /usr/local/bin/
          chmod +x /usr/local/bin/makerom
          
          wget -q https://github.com/carstene1ns/3ds-bannertool/archive/refs/heads/master.zip -O bannertool-src.zip
          unzip -q bannertool-src.zip
          cd 3ds-bannertool-master
          make
          cp output/linux-x86_64/bannertool /usr/local/bin/ || cp bannertool /usr/local/bin/
          chmod +x /usr/local/bin/bannertool
          cd ..

      - name: Build
        run: make

      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            TriCord.cia
            TriCord.3dsx
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
